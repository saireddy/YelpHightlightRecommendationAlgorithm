The project was developed using eclipse IDE. Hence the preferred way of viewing the source code would be eclipse.
Attached is the zip file exported using eclipse export wizard. If using eclipse it can be directly imported(Import > 
Archive File > select the zip file)

The class CYlpDocumentHighlight has getHighlightDoc(String doc, String query), unit test for the class are present 
in CYlpDocumentHighlightTest. testGetHighlightDoc() has 8 test cases. Test case 8 is the one mentioned in the code test.
To test getHighlightDoc() user can run the unit test and pass in different test strings.

In all there are ten unit tests in the project. 

Alternatively the entire project is also compressed and attached as .tar.gz 